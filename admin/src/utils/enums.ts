export enum HandlerTypeEnum {
    Email = "email",
    Action = "action",
    Remote = "remote",
}

export enum EmailDataMessageEnum {
    Default = "default",
    Custom = "custom",
}

export enum FieldTypeEnum {
    Text = "text",
    Checkbox = "checkbox",
    Radio = "radio",
    Textarea = "textarea",
    Email = "email",
    Number = "number",
    Select = "select",
}

export enum FieldDirectionEnum {
    Up = 'up',
    Down = 'down',
}

export enum FieldActionsEnum {
    Add = 'add',
    Edit = 'edit'
}
